# Blog

Immutable writing under `content/sources/entries/` with `type: post`.

Supports a single `.md` file or a folder with `index.md` (and optional assets).

See [docs/entries.md](/docs/entries) for packaging details.
